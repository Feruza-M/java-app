services:
  java-app:
    build: .
    container_name: java-app
    restart: always

  nginx:
    image: nginx:alpine
    container_name: java-nginx
    ports:
      - "8085:80"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - java-app
    restart: always
